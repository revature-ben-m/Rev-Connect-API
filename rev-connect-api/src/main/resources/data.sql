/* CREATE TABLE users (
    u_id VARCHAR(255) UNIQUE NOT NULL,
    name VARCHAR(255),
    bio VARCHAR(255),
    PRIMARY KEY (u_id)
);

INSERT INTO users (u_id, name, bio) VALUES ('1', 'John Doe', 'John bio'); */